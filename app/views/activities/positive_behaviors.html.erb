<div class="content mdl-color-text--grey-800 mdl-cell mdl-cell--8-col">

<h4>Instead of thinking <%= concern.text %>, I have the ability to</h4>

<div class="positive_behaviors">
  <% category.positive_behaviors.each do |positive_behavior| %>
    <%= render partial: 'emoji_list_item', locals: {entry: positive_behavior, type: 'positive_thought', link_url:success_plan_activity_path(positive_behavior: positive_behavior.id) } %>
  <% end %>
</div>
